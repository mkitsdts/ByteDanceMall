{
    "sql_config": [
        {
            "host": "localhost",
            "port": "3306",
            "user": "root",
            "password": "root",
            "database": "user"
        }
    ],
    "redis_config": {
        "address": [
            "localhost:6379"
        ],
        "password": "root"
    },
    "authserver_config": {
        "address": "localhost:50051",
        "default_user": "world"
    }
}