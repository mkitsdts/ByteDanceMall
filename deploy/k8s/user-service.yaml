apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: your-namespace
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
  selector:
    app: user
